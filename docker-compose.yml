version: '3'

services:
  app: &default
    build:
      context: .
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
  test: &test
    <<: *default
    command: python manage.py test
